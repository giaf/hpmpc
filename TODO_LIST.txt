AVX kernel:
- _mm_mul & _mm_add instead of += * (add support to icc)
- register renaming & ooo in kernels
- pack to simd size
- flag for dtrmm kernel
(- faster and lower accuracy kernels for dpotrf and dtrsv)

Add other kernels (SSE, NEON, C99, POWERPC)

BLAS:
- dtrmm_pu & dpotrf_p_dcopy_u
- dpotrf(m, n) to factorize only a band of C
(- code generation version ?)
- use -DTARGET

LQCP & IP
- avoid copy of Q in IP
- free x0 vs fixed x0
- take out lagrangian multipliers
- optimized condensing
