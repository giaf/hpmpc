
SET( SRCS d_aux_ip_c99_lib4.c s_aux_ip_c99_lib4.c )

FOREACH( SRC ${SRCS} )
    GET_FILENAME_COMPONENT(ABS_SRC ${SRC} REALPATH)
	SET( SRC_MPC_AUX ${SRC_MPC_AUX} ${ABS_SRC} )
ENDFOREACH()
SET ( SOURCES ${SOURCES} ${SRC_MPC_AUX} PARENT_SCOPE )
